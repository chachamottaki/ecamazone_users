apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  selector:
    app: my-app
  ports:
    - protocol: TCP
      port: 80
      nodePort: 30007  
      targetPort: 3000
  type: NodePort

